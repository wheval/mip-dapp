/* 'use client';

import { ChipiProvider } from '@chipi-pay/chipi-sdk';

export default function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ChipiProvider apiKey="pk_prod_670b0255b3afa5590d0d938ce5f423ed">
      {children}
    </ChipiProvider>
  );
}  */